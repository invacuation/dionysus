services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DIONYSUS_ENVIRONMENT: development
      DIONYSUS_DATABASE_URL: ${DIONYSUS_DATABASE_URL:-sqlite:////app/var/dionysus.db}
      DIONYSUS_BOOTSTRAP_ADMIN_USERNAME: ${DIONYSUS_BOOTSTRAP_ADMIN_USERNAME:-admin@example.local}
      DIONYSUS_BOOTSTRAP_ADMIN_PASSWORD: ${DIONYSUS_BOOTSTRAP_ADMIN_PASSWORD:-change-me-now-please}
      DIONYSUS_BOOTSTRAP_ADMIN_DISPLAY_NAME: ${DIONYSUS_BOOTSTRAP_ADMIN_DISPLAY_NAME:-Local Admin}
    volumes:
      - ./var:/app/var
    depends_on:
      db:
        condition: service_healthy
        required: false

  db:
    image: postgres:16
    profiles: ["postgres"]
    environment:
      POSTGRES_USER: dionysus
      POSTGRES_PASSWORD: dionysus
      POSTGRES_DB: dionysus
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dionysus -d dionysus"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
